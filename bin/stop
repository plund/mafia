#!/bin/bash

PID=$(ps aux | grep -vE "(grep|to_erl|run_erl|bin/stop)" | grep mafia \
          | awk '{print $2}')

if [ "$PID" != "" ] ; then
    echo "Stopping Mafia server on PID=${PID}"
    kill -9 $PID
else
    echo No PID
fi

exit 0
