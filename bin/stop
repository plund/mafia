#!/bin/bash

PID=$(ps aux | grep -vE "(grep|to_erl|run_erl)" | grep mafia | awk '{print $2}')

if [ "$PID" != "" ] ; then
    echo Stopping Mafia server on PID=$PID
    kill $PID
else
    echo No PID
fi

exit 0
